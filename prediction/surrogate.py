def compose_prediction(job_config, mechanism, job_features):
    # Placeholder: return (y_hat, v_hat, dro_band) for a given config and mechanism
    pass

def knn_lookup(job_config, job_features, k):
    # Placeholder: return k nearest PerfDB or EvidenceStore rows for given features
    pass

def predict_v_trajectory(job_config, mechanism_id, window):
    # Placeholder: return list of v_hat_t predictions over window
    pass

def compute_req_metrics(job_features):
    # Placeholder: return required tokens/second for given workload features
    pass
